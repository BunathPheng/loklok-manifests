---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loklok                  # Ingress name
  labels:
    app: loklok
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "loklok.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loklok               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
